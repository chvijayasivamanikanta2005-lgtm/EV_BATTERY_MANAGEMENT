# No Git LFS — all files tracked as standard git objects.
# Binary files marked as binary to prevent line-ending changes.

*.keras binary
*.pkl binary
*.h5 binary
*.joblib binary

# Normalize line endings for text files
* text=auto
